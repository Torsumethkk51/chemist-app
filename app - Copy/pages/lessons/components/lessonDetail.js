export default function LessonDetail(props) {
    const { setLessonDetailElement } = props

    return (
        <div className="fixed top-0 left-0 right-0 bottom-0 z-[999] bg-black/50 backdrop-blur-[5px] w-full h-[100vh] flex justify-center items-center" onClick={() => {setLessonDetailElement(null)}}>
            <ol>
                sdfsdfsd;fsdf
            </ol>
        </div>
    )
}